# Task Completion Checklist

When completing a coding task, run these commands in order:

1. **Format code**: `go fmt ./...`
2. **Run tests**: `go test ./...`
3. **Lint code**: `golangci-lint run` (if available)
4. **Build project**: `go build ./...`

Ensure all commands pass before considering the task complete.